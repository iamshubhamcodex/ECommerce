import Section from "../Section";

const Membership = () => {
  return (
    <Section heading="Membership">
      <img src="/membership.png" className="w-full my-2 h-auto" />
    </Section>
  );
};

export default Membership;
